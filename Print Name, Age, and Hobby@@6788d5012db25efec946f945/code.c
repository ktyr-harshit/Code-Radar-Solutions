#include <stdio.h>
int main() {
    char n[200],a[200],h[200];
    scanf("%s", n);
    scanf("%s", a);
    scanf("%s", h);

    printf("Name: %s\nAge: %s\nHobby: %s", n, a, h);
    return 0;
}
